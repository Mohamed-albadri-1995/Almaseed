# النسخة المرجعية المستقرة — أرشيف المسيد

هذه هي النسخة المرجعية المعتمدة (stable reference). عند حدوث أي خلل في نسخة
أحدث، نعود إلى هذه النقطة.

## التطبيق (Android)
- **BUILD: 58** — `versionCode` 57
- Commit: `97ca602` (fix(mobile): keep search keyword on back + add autocomplete)
- بناء APK: GitHub Actions run **34676938858**
- يشمل كل ما في المرجع السابق (بناء 50) بالإضافة إلى:
  - **بحث محسّن:** بقاء كلمة البحث عند الرجوع من مادة، إكمال تلقائي للاقتراحات،
    زر مسح البحث.
  - **تسجيل دخول Google بضغطة واحدة** عبر متصفح النظام (بلا كتابة كلمة المرور).
  - **معاينة المقطع قبل الرفع** + إصلاح التسجيل داخل التطبيق (مدائح/محاضرات).
  - **فلاتر النوع/الأقسام** قابلة للإلغاء مع فصل بصري واضح.
  - **فكّ رموز HTML** (بما فيها المزدوجة) في كل الحقول.
  - أسماء تنزيل نظيفة موسومة بالمصدر، عرض الصوت بشعار السجادة، تصنيف صحيح للمقالات.

## الخادم / الموقع
- Commit مرجعي للخادم: `d9710a5` (أحدث إصلاحات الخادم على هذا الفرع)
- يشمل كل ما سبق بالإضافة إلى:
  - **الختم الكامل:** الشعار + اسم المساهم + `almaseeed.com` على الصور/PDF/الفيديو/
    غلاف الصوت (نصّ مرسوم كصورة ليُشكّل العربية بشكل صحيح؛ خطوط Noto في Docker).
  - **حفظ الأصل قبل الختم** (`originalFileUrl`/`originalCoverImage`) — إعادة الختم
    لا تُنتج ختمًا مزدوجًا، وحذف تلقائي للنسخ المختومة القديمة.
  - **تنظيف رموز HTML من قاعدة البيانات** عبر `/api/clean-entities?run=1` +
    تنظيف عند الحفظ (الإرسال/التعديل) لمنع تكرار المشكلة.
  - **فلتر «مقالات»** = مادة بلا ملف (`fileUrl = null`) — لا تتسرّب إليه الصوتيات.
  - نقطة اقتراحات البحث للتطبيق `/api/mobile/search/suggest`.
  - تنزيل حقيقي (بث + Content-Disposition)، العامل الخلفي للختم، إشعارات المراجعين.

## للرجوع إلى هذه النسخة
```
git checkout -B claude/website-app-version-audit-1rdtdj 97ca602   # للتطبيق (بناء 58)
# أو للخادم فقط أعد نشر Railway من d9710a5
```
> ملاحظة: أرقام البناء الأحدث تُبنى فوق هذه النسخة؛ إن ظهر خلل نعود إلى BUILD 58.

## المرجع السابق (احتياطي)
- BUILD 50 — `versionCode` 49 — Commit `bf17105` — خادم `faa52a6`.
