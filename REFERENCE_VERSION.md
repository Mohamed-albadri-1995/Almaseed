# النسخة المرجعية المستقرة — أرشيف المسيد

هذه هي النسخة المرجعية المعتمدة (stable reference). عند حدوث أي خلل في نسخة
أحدث، نعود إلى هذه النقطة.

## التطبيق (Android)
- **BUILD: 50** — `versionCode` 49
- Commit: `bf17105` (feat: sign in with Google via web redirect)
- بناء APK: GitHub Actions run **34652978434**
- يشمل: الشعار الموحّد، رفع صوت/فيديو فقط للأقسام المخصّصة، إشعارات داخل التطبيق
  + إشعارات النظام (Firebase/FCM)، تسجيل الدخول (بريد/كلمة مرور + Google + نسيت
  كلمة المرور)، خطوط مطابقة للموقع، أسماء تنزيل نظيفة موسومة بالمصدر، عرض الصوت
  بشعار السجادة، تصنيف صحيح للمقالات، فكّ رموز HTML.

## الخادم / الموقع
- Commit مرجعي للخادم: `faa52a6` (fix build: watermark worker كعملية مستقلة)
- يشمل: تنزيل حقيقي (بث + Content-Disposition)، العامل الخلفي للختم
  (صور/PDF/فيديو/صوت)، إشعارات المراجعين، تسجيل الدخول عبر Google (ويب + تطبيق)،
  فكّ رموز HTML، فلترة نوع المحتوى.

## للرجوع إلى هذه النسخة
```
git checkout -B claude/website-app-version-audit-1rdtdj bf17105   # للتطبيق
# أو للخادم فقط أعد نشر Railway من faa52a6
```
> ملاحظة: أرقام البناء الأحدث تُبنى فوق هذه النسخة؛ إن ظهر خلل نعود إلى BUILD 50.
