# مسائل معروفة

## 0) تجمّد التطبيق (ANR) عند التنزيل ✅ (أُصلحت في BUILD 69)
- **السبب:** الكتابة إلى مجلد SAF مُنتقى تتطلّب نسخ الملف كاملًا كنصّ Base64 عبر
  جسر JS، وهذا يحجب الخيط الرئيسي مع الوسائط الكبيرة فيتجمّد التطبيق («isn't
  responding»)، وقد يستنزف الذاكرة. لا توجد واجهة بثّ (streaming) للكتابة إلى مجلد
  SAF في هذه النسخة من expo-file-system (تحقّقتُ من الكود الأصلي).
- **الإصلاح:** الوسائط (صوت/فيديو/صورة — وهي جوهر الأرشيف) تُحفَظ **محلّيًا عبر
  MediaLibrary** (بثّ أصلي، سريع، بلا تجمّد ولا Base64). المستندات فقط (صغيرة) هي
  التي تستخدم مُنتقي المجلد.

## 1) «تنزيل إلى الجهاز» ✅ (BUILD 69)
- **الوسائط (صوت/فيديو/صورة):** تُحفَظ في المعرض/الموسيقى مباشرةً (بلا قائمة مشاركة،
  بلا تجمّد).
- **المستندات/المقالات (صغيرة):** يفتح مُنتقي مجلدات النظام (SAF) لتختار مجلدًا
  ويُحفَظ فيه، ونتذكّر المجلد المختار (يمكن تغييره عبر «تغيير مجلد الحفظ»).
- **آلية كتابة المستند:** `StorageAccessFramework.createFileAsync` ثم الكتابة. لا
  نستخدم `copyAsync` إلى وجهة `content://` لأنها تستدعي `toFile()` على الوجهة فتُخفق.

## 2) اتجاه شريط السحب (Seek) ✅ (BUILD 68)
- **الحقيقة المُثبَتة بالتجربة (لقطة build 67):** التطبيق يفرض RTL عامًّا، وأندرويد
  **يتجاهل** `direction:'ltr'` على View مفردة، فيُعكَس الشريط: التعبئة/المؤشّر
  (left:%) تُرسَم من الحافة **اليمنى** والزمن المنقضي يظهر يمينًا. لكن إحداثي اللمس
  يبقى ماديًّا من اليسار.
- **الإصلاح:** نعكس اللمس بـ `1 - raw` تحت RTL ليطابق التخطيط المعكوس، فيتبع
  المؤشّر الإصبع تمامًا ويصبح السحب طبيعيًّا. (النسخة المرجعية بدت صحيحة فقط لأنها
  لم تكن تفرض RTL أصلًا، فلم يحدث أي عكس.)
- ملاحظة: التعبئة تنمو من اليمين (منطقي في الواجهة العربية). لو أردناها من اليسار
  كمشغّل إنجليزي، نحتاج قلبًا بصريًّا صريحًا (transform) — غير مطبَّق حاليًا.
